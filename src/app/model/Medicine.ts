export class Medicine {

  name: string;
  date: string;
  duration: number;


  constructor(name: string, date: string, duration: number) {
    this.name = name;
    this.date = date;
    this.duration = duration;
  }
}
